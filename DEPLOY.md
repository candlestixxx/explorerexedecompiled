# DEPLOY

## Pipeline Execution
Run the full 6-phase ingestion to compilation pipeline via:
`./run_all.sh path/to/binary.exe`

## Git Sanitization
Always run `./sync.sh` before development to handle upstream pulling, branch rebasing, and recursive submodule syncing.
